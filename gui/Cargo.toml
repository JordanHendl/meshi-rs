[package]
name = "meshi-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
meshi-graphics = { path = "../graphics" }
glam = { version = "0.29.2", features = ["bytemuck"] }

# GUI stack versions compatible with winit 0.26.x.
egui = "0.18.0"
egui-winit = "0.18.0"
winit = "0.26.1"
